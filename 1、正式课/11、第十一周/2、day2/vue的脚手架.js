// npm i @vue/cli -g   4
// vue create 项目名字

// npm i vue-cli -g     vue init webpack exp


let a = 100;
let b = 200;

    // esLint: 代码，校验代码的格式是否正确；会校验你的代码书写规范
    // shift+alt+f

    // gitIgnore:git 忽略
    // babel.config:配置es6的
    // README.md:对项目的一个描述和解析
    // src:资源文件，我们写的代码都在src里面
    // node_modules:安装的模块依赖
    // public：webpack打包之后的文件会自动放到html中
    // dist:文件在yarn build之后产生的，我们的整个项目上线之前需要进行打包，这个dist就是打包之后的文件